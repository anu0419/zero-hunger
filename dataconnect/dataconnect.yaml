specVersion: "v1beta"
serviceId: "zerohungerr"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "zerohungerr-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
